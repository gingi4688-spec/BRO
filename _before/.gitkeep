# placeholder — keeps _before/ tracked (empty). Snapshots (.zip + sha256) before critical writes (§13A). Empty in Phase 0.
# placeholder — _before/-ը track (դատարկ)։ Snapshot-ներ critical write-ից առաջ (§13A)։ Phase 0-ում դատարկ։
