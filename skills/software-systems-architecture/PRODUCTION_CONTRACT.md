# software-systems-architecture — PRODUCTION CONTRACT / PRODUCTION CONTRACT

> **STATUS: PROVEN (proof slice 2 — Code).** Validated end-to-end on a real pure-logic TypeScript change in a throwaway sandbox → runtime GREEN: TDD red→green (13/13), `tsc --noEmit` + `eslint` + `tsc` build all exit 0, a controlled boundary defect caught+repaired+re-verified, plus one unplanned build-config defect (TS5011) the build gate caught. Independent evidence (exit codes + test output) is recorded in [`memory/_evidence/EVIDENCE_LEDGER.md`](../../memory/_evidence/EVIDENCE_LEDGER.md) (CODESLICE-001), [`memory/_evidence/VERIFICATION_LEDGER.md`](../../memory/_evidence/VERIFICATION_LEDGER.md), and [`memory/_failures/FAILURE_LEDGER.md`](../../memory/_failures/FAILURE_LEDGER.md) (FL-008, FL-009). Bro now holds TWO proven adapters (UI + Code); Universal Core extraction is thereby honest — but is its own phase and the Universal Core label stays DECLARED until extracted+validated.
>
> **ԿԱՐԳԱՎԻՃԱԿ՝ ՊՐՈՎՎԱԾ (proof slice 2 — Code)։** Validate արած իրական pure-logic TypeScript change-ով throwaway sandbox-ում → runtime GREEN՝ TDD red→green (13/13), `tsc` + `eslint` + build exit 0, controlled boundary defect caught+repaired+re-verified, + մեկ չնախատեսված build-config defect (TS5011), որ build gate-ը բռնեց։ Անկախ evidence-ը (exit code + test output) գրանցված է ledger-ներում (CODESLICE-001, FL-008, FL-009)։ Bro-ն հիմա ունի ԵՐԿՈՒ ապացուցված adapter (UI + Code). Universal Core-ի extraction-ն այժմ ազնիվ է, բայց առանձին phase է, ու Universal-ի պիտակը մնում է DECLARED մինչ extract+validate։

## The 10 answers (PROVEN) / 10 պատասխանը (ապացուցված)

1. **Artifacts** — code changes, ADRs, service-boundary maps, integration/saga designs, API/event contracts, resilience plans. / code change, ADR, boundary map, contract, resilience plan։
2. **Inputs** — the change intent · the affected system · constraints · existing tests. / change intent · system · constraint · test-եր։
3. **Evidence** — the actual diff + passing tests + typecheck + lint + build + a known-limits note. / diff + test + typecheck + lint + build + known-limits։
4. **Verification gate** — `tsc`/compiler + test suite + lint + build (wired recipe: [`_core/production_os/code/verification/CODE_VERIFICATION_CHECKLIST.md`](../../_core/production_os/code/verification/CODE_VERIFICATION_CHECKLIST.md)); for design artifacts, a review against stated criteria + trade-off record.
5. **GREEN** — tests pass + typecheck + lint + build + diff summary + no unrelated changes + rollback noted (independent run, L18).
6. **YELLOW** — builds but a required check missing/blocked, or design lacks a trade-off record (named).
7. **RED** — tests/typecheck/build fail · unrelated changes rode along · GREEN claimed without a run.
8. **Approval-required (D0)** — push · migrate · release · lock architecture · touching prod. Gev-gated.
9. **Refusal / boundary** — no "should pass" as proof · no scope creep in a fix · no silent breaking change.
10. **Eval cases (RUN — proof slice 2)** — module `billingPeriod.ts` (pure UTC date math, 51 LOC, no DOM/I-O/clock). **PASS:** 13 edge-case tests (inclusive-boundary day count · leap-day span 2024 · invalid-calendar rejection `2026-02-30` · malformed + out-of-range month · end-before-start throw) — stub red **13/13 fail** → impl green **13/13 pass**; `tsc --noEmit`=0, `eslint`=0, `tsc -p tsconfig.build.json`=0 → `dist/billingPeriod.js`+`.d.ts`. **FAIL (controlled):** `isWithinPeriod` end made exclusive (`value <= end` → `value < end`) → exactly **1** test caught it (`includes the end date … expected false to be true`), repaired to inclusive → re-verify GREEN (**FL-008**). **EDGE (unplanned):** first `build` failed **TS5011** (missing `rootDir`) — the build gate caught it, fixed via `"rootDir": "src"` (**FL-009**). Toolchain: node 22.22.3 · tsc 6.0.3 · vitest 4.1.9 · eslint 10.6.0. Sandbox origin: `scratchpad/phase9-code-slice/` (16 evidence files).

## Why this is now PROVEN (and Universal is not yet) / Ինչու է սա հիմա PROVEN (իսկ Universal-ը՝ դեռ ոչ)

The word "universal" is earned only from **two** proven adapters (UI + Code) — that bar is now met. This contract flipped to PROVEN because a real code change actually ran to runtime GREEN with independent evidence (exit codes + test output), not off zero runs — the opposite of the fake-GREEN L16/L18 forbid. Universal Core extraction is now honest, but extraction is a separate phase and the Universal Core label stays DECLARED until the shared core is lifted from both slices and both re-validate — proving generality is measured (L17), not a byproduct of the second slice. / «Universal»-ը վաստակվում է 2 ապացուցված adapter-ից — էդ բարը հիմա բավարարված է։ Այս contract-ը PROVEN դարձավ, որովհետև իրական code change-ը փաստացի runtime GREEN հասավ անկախ evidence-ով, ոչ զրո run-ով։ Universal Core-ի extraction-ն այժմ ազնիվ է, բայց առանձին phase է, ու Universal-ի պիտակը մնում է DECLARED մինչ shared core-ը հանվի երկու slice-ից ու երկուսն էլ re-validate լինեն (L17՝ չափված, ոչ 2-րդ slice-ի կողմնակի արդյունք)։
