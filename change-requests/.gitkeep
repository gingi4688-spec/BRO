# placeholder — keeps change-requests/ tracked (empty). One CR-<ts>-NNN.md per critical action (§13A). Empty in Phase 0.
# placeholder — change-requests/-ը track (դատարկ)։ CR ամ. critical action-ի (§13A)։ Phase 0-ում դատարկ։
